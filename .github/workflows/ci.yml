name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - run: npm install --no-audit --no-fund
      - run: npm run check
      - run: npm run build
      - run: npm run generate:acceptance
      - run: npm install --no-audit --no-fund
        working-directory: .tmp/generated-acceptance
      - run: npm run check
        working-directory: .tmp/generated-acceptance
      - run: npm run build
        working-directory: .tmp/generated-acceptance
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e
