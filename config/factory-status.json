{
  "schemaVersion": 1,
  "$comment": "The machine-readable answer to 'what is happening now?'. It is not a historical database: completed stages are ids only, and the narrative for anything closed lives in Git. docs/ROADMAP.md sequences; this file states.",
  "currentPhase": "4D",
  "currentStage": "Phase 4D — Visual direction, responsive composition and candidate promotion",
  "status": "active",
  "blockedBy": "The Presentation Registry is the visual ceiling, measured rather than argued. Three interventions this session each moved the number they targeted and none closed the gap. A distinctive moment now has to reserve layout in every variant its section can compile to; an art-direction axis now has to reach the stylesheet or stop being counted for diversity (25 of 36 declared axis values did not render, and three of the ten axes the diversity gate counts rendered nothing at all); and a new direction, schedule-register, composes a numbered document register rather than cards. Result on the fourth independent GPT-5 review over one set of three candidates from the same frozen pack e7c387bc: schedule-register 6.55, the best recorded and up from 5.89, with distinctive-moment 2.5 -> 5.2, responsive-quality 7.0 and floor 3.8 -> 4.8. Still rework, still far from 8.5. The critic names the cause: apart from the numbered rows the design relies on familiar sans-serif headings, pill buttons, thin dividers and a dark CTA rectangle. A direction can change tokens, section variants and a few section treatments. It cannot change what the page is made of, because every direction draws the same component vocabulary. That is the next architectural intervention and it is not another CSS pass.",
  "activeWork": [
    "phase-4d-presentation-registry-ceiling",
    "phase-4d-richer-business-input",
    "phase-4-2a-static-visual-review"
  ],
  "outstandingProductGates": [
    "phase-4d-presentation-registry-ceiling: the one structural blocker Phase 4D has left, and the first one measured against a build whose declared art direction actually renders. Verdicts at examples/genuine-business/nbm-visual-review-v4.verdicts.json, bound to composition 3c8db759 and 21 captures for the challenger. Three candidates, one frozen pack, one review: structured-practice 6.05 rework, editorial-authority 5.74 REJECT, schedule-register 6.55 rework. The interventions worked and are measurable - distinctive-moment 2.5 -> 5.2, responsive-quality 4.2/6.3 -> 7.0, mean 5.89 -> 6.55, floor 3.8 -> 4.8 - and the ceiling did not move, because distinctiveness stayed at 4.8 across every candidate in every review since v2. CLASSIFICATION: Presentation Registry ceiling, not art-direction compilation. Art direction now compiles; what it compiles onto is one component vocabulary. Every direction draws the same pill buttons, the same dark CTA rectangle, the same thin dividers and the same sans-serif display face, because a direction owns token values and section variants and owns no components. schedule-register could turn cards into ruled rows and could not stop the closing ask being a dark rectangle or choose a typeface. THE INTERVENTION: let a direction select component implementations, not only restyle one set - starting with the two the critic named in all four reviews, the CTA block and the button, and with a per-direction display typeface. Until a direction can change what the page is made of, every candidate is a lightly branded template and no amount of CSS on top of a fixed vocabulary reaches 8.5. Do not attempt this as another candidate-tuning pass.",
    "phase-4-2a-static-visual-review: the genuine nbm business renders through the static/content renderer as an independent repository with zero client JavaScript and six route documents. `npm run acceptance:static-renderer` leaves the evidence at .app-builder/static-renderer and stops. A reviewer who did not produce it must say whether the static rendering is at least as good as the application-rendered one before 4.2A closes.",
    "professional-output-independent-verdict: the one outstanding row in docs/VISUAL_EXCELLENCE.md §9. It is the same gate three times over, because a candidate set, a static rendering and any bespoke presentation each need a reviewer who did not produce the work.",
    "nbm-source-pack-ceiling: the frozen nbm case may not be able to reach the 8.5 bar without fabricating facts, and that is a corpus finding rather than a factory defect. Both independent reviews ask for the same missing material - project experience, credentials, qualifications, sectors, team information, client outcomes, process - and buildContract.explicitlyExcluded forbids exactly that: named client or project case studies, republished photographs, and any performance, client, award, accreditation or experience claim no approved source supports. The approved pack is a company name, one registry sentence, four service names with no descriptions, two location names with no detail, one phone number and one address; entities is empty. A typography and layout led direction has a ceiling on that much content and no art direction raises it. Decide deliberately whether the bar is proved on a richer business, whether the owner can approve more nbm material, or whether the bar is wrong for a business this thinly evidenced. Do not close the gap by inventing proof. Measured on the 2026-08-28 rerun: the factory itself now quantifies part of this. The candidate generator REFUSED the immersive-lead direction outright - 'imagery-not-available ... No photographs were supplied at all, so the direction has to carry the page with type and layout' - and assetReadiness records strategy 'typography-led' with supportsImageryLed false. One of three visual directions is unreachable for this business on rights grounds alone, before any judgement of taste. That is a source ceiling the factory measures rather than an opinion about it, and it is the first row of the source-adequacy evidence a second business is needed to turn into a relationship. One row is not a mechanism; do not build a SourceAdequacy subsystem on it. Corrected 2026-08-28: the claim that the approved pack holds no service descriptions and no email address was true of the manifest and false of the sources. Both are present once the declared sources are ingested. The ceiling that survives measurement is proof and imagery - no people, projects, testimonials, accreditations or publishable photographs - not content thinness in general.",
    "phase-4d-richer-business-input: the experiment Phase 4D now needs is the complete candidate flow on a genuine business with materially richer approved truth, so a poor score can be attributed to the factory rather than to the source pack. The repository holds exactly one genuine business - nbm - and no second approved-intake bundle, ingested pack or durable project exists anywhere in it. What is missing is not code. It is a real company's supplied material plus an explicit rights approval, which is the entry condition every corpus case in docs/GENUINE_BUSINESS_ACCEPTANCE.md has to meet. Obtain one business from the corpus list - local trade with project photography is the easiest to source and the furthest from nbm - and freeze it in the shape examples/genuine-business/nbm-approved-intake.v1.json already defines. Until that exists the visual bar cannot be separated from the source ceiling, and neither answer can be trusted.",
    "phase-4d-enquiry-failure-state: partly done and honestly recorded. The failure is now modelled as an outcome rather than a string, keeps everything the visitor typed so retrying is pressing the button again, and names the alternative rather than saying try again. It was given no visual treatment, so the v4 critic still asks for stronger visual status and a more deliberate relationship to the submit control on all three candidates. There is no status colour in the token set; adding one is a DesignSystemSpec change and belongs with the component work above rather than as a loose CSS rule.",
    "phase-4d-visible-navigation-treatment: the clipping is fixed and the treatment is still not right. inline-scroll lost destinations off-screen and two reviews called it clipped; inline-wrap shows all five at 390px and v4 calls it an awkward loose second row, though editorial-authority responsive-quality rose 4.2 -> 5.8. Both disclosure candidates scored 6.3 and 7.0 on the same axis. Either design the visible row as a deliberate two-row bar or retire the treatment and let disclosure be the one mobile navigation, but decide it on this evidence rather than by adding a third scroller."
  ],
  "nextStage": "The product-proof freeze and the varied real-business corpus in docs/GENUINE_BUSINESS_ACCEPTANCE.md, which opens when a candidate legitimately clears the visual bar. Everything the freeze was waiting on that could be built has been built.",
  "completedPhases": [
    "0",
    "1",
    "2",
    "3"
  ],
  "completedStages": [
    "3.5A",
    "3.5B",
    "3.6A",
    "3.6B",
    "3.7",
    "3.8A",
    "3.8B",
    "3.8C",
    "3.8D",
    "3.8E",
    "3.8F",
    "3.8G",
    "3.8H",
    "3.8I",
    "3.8J",
    "3.8K",
    "4A",
    "4B.1",
    "4B.2",
    "4B.3a",
    "4B.3b",
    "4B.3c",
    "4B.4",
    "4B.5",
    "4B.6",
    "4C",
    "4C.1",
    "4C.2",
    "4C.3",
    "4C.4",
    "4C.5",
    "4D.1",
    "4D.2",
    "4D.4",
    "4D.5",
    "4D.6",
    "4D.7"
  ],
  "recentlyCompleted": [
    "Phase 4D presentation invariants and the first genuinely new direction. Two vacuous consumers closed with planted negatives: a distinctive moment that rendered as a corner glyph on the variant richer content earned, and three diversity-counted axes that reached the stylesheet in no form at all. A fourth independent review over three candidates from one frozen pack produced the best result recorded, 6.55, and identified the remaining ceiling as the component vocabulary rather than the art direction. Mobile navigation no longer clips any destination at 390px; schedule rows stack rather than setting descriptions one word per line",
    "Phase 4D source-backed rerun - the decisive experiment. replayIntakeBundle can now carry a frozen approved knowledge pack and the candidate lane passes one, so composition is no longer starved: services went from four bare manifest names rendered as a list to four source-backed entries with descriptions and entity ids rendered as cards, contact gained info@nbm.bz so all three declared conversion goals resolve, and warnings fell from three to one. The one that survives, no-publishable-imagery, is the part of the nbm ceiling that is genuinely the source pack. A guard now refuses to mint candidate evidence when a manifest declares approved sources and the composition is still knowledge-pack-not-provided. 42 captures, 42 distinct content hashes, 6 routes x 3 viewports per candidate, launch readiness launchable with 0 blockers and 6 predicted edits all in imagery, hard constraints 1 enforced 3 routed 0 unclassified, accessibility 6/6 with no serious or critical WCAG A/AA violations",
    "declared-requirement consumers - conversion goals and hard constraints. primaryAction() collapsed call, email and contact form into one Call button on every route; conversionPlan() now resolves every declared goal and places channels by context, and nbm ships two channels plus a recorded gap where its sources hold no email address. manifest.constraints.hard had no reader at all and now has a coverage ledger that reports what enforces each constraint - 1 enforced, 3 routed to the producer that owns the evidence, 0 unclassified on nbm",
    "nbm rerun on the fixed factory - 2 candidates, diversity distinct, 21 captures each with 21 distinct content hashes, DesignLint clean, route identity intact at desktop/tablet/mobile. The #163 capture fix and the #165 responsive fixes both survive a build that was not the one they were written against",
    "first independent Phase 4D visual verdict — an OpenAI GPT-5 design-critic reviewed the nbm candidate set an Anthropic runtime created, and rejected both. The quality system worked: it found a defect the creator had missed and nothing was promoted",
    "route-capture root cause — the evidence server answered every prerendered route with the home document, because it served a path only when that path was itself a file and a prerendered build answers /services with services/index.html. Six routes per viewport were photographed as one page. Reproduced against the frozen nbm inputs, fixed in document resolution rather than by a per-project special case, and proved by six distinct captures whose rendered page identity matches the route requested",
    "4.2A implementation — static/content renderer; deterministic fail-closed renderer selection, six route documents and zero client JavaScript for the business the application renderer serves with one. Its independent visual review is outstanding above, so the stage is not closed",
    "cross-browser visual portability lane; found and fixed a real defect on its first hosted run — the site header was not sticky on any phone",
    "cross-build anti-template diagnostic; advisory by declaration, and it reports the cause of its own findings",
    "controlled bespoke-presentation lane; project-local, token-only, section-scoped, durable across a rebuild and re-validated against the build it lands in"
  ],
  "deferredCapabilities": [
    {
      "stage": "4C.6",
      "title": "Deterministic design-intelligence catalogue",
      "reason": "No component asks \"which reviewed design patterns fit this project?\". BrandSpec reads the knowledge pack's own brand observations, ArtDirectionPlan reads config/layout-patterns.json, and the Presentation Registry is compiled from what the template renders. A catalogue built now would have one producer and no reader.",
      "revivesWhen": "A real 4D consumer cannot answer a project/industry/intent pattern question from deterministic config. Then document that consumer first, and implement only the smallest lexical (BM25/keyword) catalogue it needs — no vector store, no embeddings, no generic design corpus.",
      "recordedAt": "2026-08-26"
    },
    {
      "stage": "4D.3",
      "title": "MessagingPlan",
      "reason": "The composition already carries what a MessagingPlan would organise, with provenance. Page narrative is the section sequence; CTA strategy is derived from the declared conversion goals; proof requirements and their gaps are `declaredProofGap`; audience is on the manifest; forbidden claims are the existing source/provenance boundary. A second narrative authority would have fields nothing reads and a second place the same decision could be made.",
      "revivesWhen": "An art-direction or composition decision needs narrative information the composition cannot express — a proof sequence that does not follow from what is available, or an objection-handling order. Implement only the fields that decision reads.",
      "recordedAt": "2026-08-26"
    },
    {
      "stage": "4D-canvas",
      "title": "Visual comparison canvas",
      "reason": "The ordinary Builder Console does the comparison: its candidate panel shows every candidate side by side, switches route and viewport over the captured evidence, names the axes that actually differ, shows the responsive plan and every DesignLint finding, exposes the scoped critic criteria, and promotes exactly one. The single usability failure found was the 330px activity sidebar cropping two full-page captures into roughly 150px columns; moving the panel to the builder stage fixed it. The failing surface was the narrow column, not the Console, so a canvas dependency would be bought against a layout problem.",
      "revivesWhen": "A concrete comparison task proves the Console cannot do it — spatial arrangement of many more candidates, or freehand annotation a reviewer genuinely needs to hand back. Record the task first.",
      "recordedAt": "2026-08-26"
    },
    {
      "stage": "4.2B-4.2E",
      "title": "Static semantic icons, Pagefind search, richer typed structured data, deterministic OG imagery",
      "reason": "Each needs a real consumer first. The structured data the static renderer emits today is exactly what the factory already supported — a WebSite object, emitted only when the site's own URL is known — because inventing typed entities from facts nothing supports would be a fabricated observation.",
      "revivesWhen": "A corpus project needs one of them and the absence is a recorded defect rather than a missing feature on a list. Implement only the one that project needed.",
      "recordedAt": "2026-08-27"
    }
  ]
}
