{
  "schemaVersion": 1,
  "currentPhase": "4D",
  "currentStage": "Phase 4D \u2014 Visual direction, responsive composition and candidate promotion",
  "activeStage": "Two lanes are open. Phase 4D.8/4D.9 \u2014 the visual review verdict on the genuine-business candidate set \u2014 is unchanged and still outstanding: the machinery is built, the evidence is captured, and no candidate is promoted. Phase 4.2A \u2014 the static/content renderer \u2014 is implemented and evidenced, and its own independent visual review of the statically rendered nbm output has not been issued.",
  "status": "active",
  "completedPhases": [
    "0",
    "1",
    "2",
    "3"
  ],
  "completedStages": [
    "3.5A",
    "3.5B",
    "3.6A",
    "3.6B",
    "3.7",
    "3.8A",
    "3.8B",
    "3.8C",
    "3.8D",
    "3.8E",
    "3.8F",
    "3.8G",
    "3.8H",
    "3.8I",
    "3.8J",
    "3.8K",
    "4A",
    "4B.1",
    "4B.2",
    "4B.3a",
    "4B.3b",
    "4B.4",
    "4B.5",
    "4B.6",
    "4B.3c",
    "4C.1",
    "4C.2",
    "4C.3",
    "4C.4",
    "4C.5",
    "4C",
    "4D.1",
    "4D.4",
    "4D.5",
    "4D.6",
    "4D.7"
  ],
  "activeWork": [
    "phase-4d-visual-direction",
    "visual-candidate-promotion",
    "behavioural-consumer-invariants",
    "phase-4-2a-static-renderer"
  ],
  "outstandingProductGates": [
    "phase-4d-visual-review-verdict: the nbm candidate set is generated, built and photographed, and no candidate is promoted. The factory created them, so the factory cannot promote one. The evidence is now reviewer-reachable rather than buried in build output: `npm run acceptance:visual-candidates` leaves durable factory state at .app-builder/visual-review, `npm run review:visual-candidates` opens the ordinary Builder Console over exactly that state, and the stage's compare view shows both candidates at three widths over six routes with the differing axes, the responsive plan, every DesignLint finding, the frozen truth, the refused direction and the scoped critic criteria. A reviewer who did not create them still has to pass, rework or reject each candidate against those criteria before Phase 4D closes.",
    "phase-4-2a-static-visual-review: the genuine nbm business now renders through the static/content renderer. It generates, installs, checks and builds as an independent repository with zero client JavaScript and six route documents against the application renderer's 218,118 bytes and one; it presents by the same promoted visual direction, carries the same shell classes and compiled tokens, passes DesignLint 0/0/0 and axe WCAG 2.1 A/AA at desktop and mobile, and is photographed at three widths over six routes plus the enquiry-failure state. `npm run acceptance:static-renderer` leaves that evidence at .app-builder/static-renderer and stops. What is outstanding is the same thing 4D is waiting on and for the same reason: the creator of a rendering may not pass its own visual review. A reviewer who did not produce it has to look at the captures and say whether the static nbm site is at least as good as the application-rendered one before 4.2A closes."
  ],
  "nextStage": "Phase 4D.8/4D.9 (the visual verdict) and Phase 4.2A's own visual review. 4.2A gave the factory a second architectural output: a project type now selects a renderer and the renderer names its template, deterministically from the project type and the enabled modules alone. Marketing and content sites are rendered statically by a pinned Astro 7.2.7 template; SaaS, consumer, internal and AI projects are unchanged on the React/Vite template, and a capability override only ever moves a project towards a fuller client runtime, never away from one. The two renderers share one presentation contract, one design system source and one element-identity derivation, so the promoted art direction, the ResponsiveCompositionPlan and the MotionContract survive a renderer change rather than being re-decided. Five reusable factory defects the static renderer exposed were fixed at their cause: a lint step that silently linted nothing in every generated project, a preview reported as running before it could serve, the Console serving its own module graph into preview frames, a dev-only diagnostics block distorting every rendered-evidence capture, and an SPA catch-all baked into the deployment adapter. 4.2B icons, 4.2C Pagefind, 4.2D typed structured data and 4.2E deterministic OG imagery are deliberately not started: each needs a real consumer first. docs/PHASE_4_2_EXECUTION.md records the measurements, the adoption reasoning and the one known limitation.",
  "deferredCapabilities": [
    {
      "stage": "4C.6",
      "title": "Deterministic design-intelligence catalogue",
      "reason": "No component asks \"which reviewed design patterns fit this project?\". BrandSpec reads the knowledge pack's own brand observations, ArtDirectionPlan reads config/layout-patterns.json, and the Presentation Registry is compiled from what the template renders. A catalogue built now would have one producer and no reader.",
      "revivesWhen": "A real 4D consumer cannot answer a project/industry/intent pattern question from deterministic config. Then document that consumer first, and implement only the smallest lexical (BM25/keyword) catalogue it needs \u2014 no vector store, no embeddings, no generic design corpus.",
      "recordedAt": "2026-08-26"
    },
    {
      "stage": "4D.2",
      "title": "Visual reference analysis",
      "reason": "No project supplies a design reference the factory does not already read. The half of reference handling that has a supplier shipped at 4C.3: BrandSpec resolves the accent and the typographic voice from the colours and font families Phase 3 observed in a company's own pages, with sources. What 4D.2 would add \u2014 layout, density, hero treatment and spacing rhythm decomposed from a screenshot or moodboard \u2014 has no input today, and inventing traits from a site nobody supplied would be a fabricated observation.",
      "revivesWhen": "A project supplies a screenshot, moodboard or approved reference URL AND a direction-selection decision exists that asset readiness and project type cannot make. Traits must then become adopt[]/avoid[] with confidence and source ids, never markup, and never bypass PageSpec/SectionSpec/ElementIdentity.",
      "recordedAt": "2026-08-26"
    },
    {
      "stage": "4D.3",
      "title": "MessagingPlan",
      "reason": "The composition already carries what a MessagingPlan would organise, with provenance. Page narrative is the section sequence; CTA strategy is derived from the declared conversion goals; proof requirements and their gaps are `declaredProofGap`; audience is on the manifest; forbidden claims are the existing source/provenance boundary. A second narrative authority would have fields nothing reads and a second place the same decision could be made.",
      "revivesWhen": "An art-direction or composition decision needs narrative information the composition cannot express \u2014 a proof sequence that does not follow from what is available, or an objection-handling order. Implement only the fields that decision reads.",
      "recordedAt": "2026-08-26"
    },
    {
      "stage": "4D-canvas",
      "title": "Visual comparison canvas",
      "reason": "The ordinary Builder Console does the comparison. Its candidate panel shows every candidate side by side, switches route and viewport over the captured evidence, names the axes that actually differ rather than leaving them to be spotted, shows the responsive plan field by field, shows what the deterministic checks settled and what the reviewer must speak to, exposes the scoped critic criteria, and promotes exactly one. The one usability failure found was the 330px activity sidebar cropping two full-page captures into roughly 150px columns; the panel moved to the builder stage and the problem went with it. The failing surface was the narrow column, not the Console, so tldraw or an equivalent would still be a dependency bought against a layout problem.",
      "revivesWhen": "A concrete comparison task proves the Console cannot do it \u2014 spatial arrangement of many more candidates, or freehand annotation a reviewer genuinely needs to hand back. Record the task first.",
      "recordedAt": "2026-08-26"
    }
  ]
}
