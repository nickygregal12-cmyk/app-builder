# Foundation v0 setup verification

This file exists to exercise the repository's pull-request CI during initial setup.

Foundation v0 is considered verified when the PR workflow completes the deterministic doctor, manifest tests, manifest validation, TypeScript check, lint and Builder Console build successfully.
